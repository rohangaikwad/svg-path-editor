import CommandListManager from "./CommandListManager";
import PathInput from "./PathInput";

const Editor = () => {
    return (
        <div>
            <PathInput />
            <CommandListManager />
        </div>
    );
};

export default Editor;
